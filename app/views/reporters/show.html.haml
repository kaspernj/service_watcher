- content_for(:header_title){ _("Show reporter: %{name}", :name => @reporter.name) }

%div{style: "padding-bottom: 15px;"}
  = link_to _("Edit reporter"), edit_reporter_path(@reporter), :class => "button"

= render "/public_activities/table", :resource => @reporter
